import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Us — OCDA",
  description:
    "Get in touch with OCDA — Ojobeda Community Development Association. Visit our office, call us, or send a message.",
};

export default function ContactPage() {
  return <ContactClient />;
}
